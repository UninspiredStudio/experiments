import type { SliderRange } from './common'

export const FRAGMENTATION_RANGE: SliderRange = { min: 0, max: 400 }
export const BRIGHTNESS_INFLUENCE_RANGE: SliderRange = {
  min: 0,
  max: 1,
  step: 0.05,
}
