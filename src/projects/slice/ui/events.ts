import type { SliceConfig } from '../types'
import type { SliceUiElements } from './elements'

export function attachSliceEventListeners(
  _elements: SliceUiElements,
  _config: SliceConfig,
): void {
  // Placeholder for wiring DOM events.
}
